Let's solve the JavaScript-related questions step by step.

---

### **Q3: Write a JavaScript program to accept a number from the user and check if it is even or not.**

```javascript
let number = prompt("Enter a number:");
if (number % 2 === 0) {
  console.log(`${number} is even.`);
} else {
  console.log(`${number} is odd.`);
}
```

---

### **Q4: Write code that creates a simple text file with the data provided by the user.**

Using Node.js (requires the `fs` module):

```javascript
const fs = require('fs');
let userData = prompt("Enter some data for the file:");

fs.writeFile('output.txt', userData, (err) => {
  if (err) throw err;
  console.log('File has been saved!');
});
```

---

### **Q5: Write a code using the `useState` Hook that displays the number of times a button named "CLICK" is clicked.**

Using React:

```javascript
import React, { useState } from 'react';

function ClickCounter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>CLICK</button>
    </div>
  );
}

export default ClickCounter;
```

---

### **Q10: Write a JavaScript program that displays four buttons named "Red," "Blue," "Green," and "Yellow." On clicking any of these buttons, the code displays the message that you have selected that particular color.**

```javascript
<!DOCTYPE html>
<html>
<head>
  <title>Color Selector</title>
</head>
<body>
  <button onclick="selectColor('Red')">Red</button>
  <button onclick="selectColor('Blue')">Blue</button>
  <button onclick="selectColor('Green')">Green</button>
  <button onclick="selectColor('Yellow')">Yellow</button>

  <p id="message"></p>

  <script>
    function selectColor(color) {
      document.getElementById('message').textContent = `You selected ${color}`;
    }
  </script>
</body>
</html>
```

---

### **Q11: Write a JavaScript program to display a digital clock on a web page.**

```html
<!DOCTYPE html>
<html>
<head>
  <title>Digital Clock</title>
</head>
<body>
  <h1 id="clock"></h1>

  <script>
    function showTime() {
      let time = new Date();
      let hours = time.getHours();
      let minutes = time.getMinutes();
      let seconds = time.getSeconds();
      let am_pm = hours >= 12 ? "PM" : "AM";

      hours = hours % 12;
      hours = hours ? hours : 12; 
      minutes = minutes < 10 ? "0" + minutes : minutes;
      seconds = seconds < 10 ? "0" + seconds : seconds;

      let currentTime = hours + ":" + minutes + ":" + seconds + " " + am_pm;
      document.getElementById('clock').textContent = currentTime;

      setTimeout(showTime, 1000);
    }

    showTime();
  </script>
</body>
</html>
```

---

### **Q12: Write a JavaScript program to display three radio buttons on the web page, namely "Red," "Blue," and "Green." Selecting any button changes the background color as per the name of the button.**

```html
<!DOCTYPE html>
<html>
<head>
  <title>Change Background Color</title>
</head>
<body>

  <input type="radio" name="color" value="Red" onclick="changeColor('red')"> Red
  <input type="radio" name="color" value="Blue" onclick="changeColor('blue')"> Blue
  <input type="radio" name="color" value="Green" onclick="changeColor('green')"> Green

  <script>
    function changeColor(color) {
      document.body.style.backgroundColor = color;
    }
  </script>

</body>
</html>
```

---

### **Q13: Create a web page using HTML and JavaScript where an image moves across the screen from left to right continuously.**

```html
<!DOCTYPE html>
<html>
<head>
  <title>Moving Image</title>
  <style>
    #myImage {
      position: absolute;
      left: 0;
      top: 100px;
    }
  </style>
</head>
<body>
  <img id="myImage" src="image.jpg" width="100" height="100">

  <script>
    let position = 0;
    let image = document.getElementById('myImage');

    function moveImage() {
      position += 1;
      image.style.left = position + 'px';

      if (position > window.innerWidth) {
        position = -100;
      }

      requestAnimationFrame(moveImage);
    }

    moveImage();
  </script>
</body>
</html>
```

---

### **Q14: Write JavaScript code to change the background color of the web page to red if the button named "RED" is clicked and to green if the button named "GREEN" is clicked.**

```html
<!DOCTYPE html>
<html>
<head>
  <title>Change Background Color</title>
</head>
<body>

  <button onclick="document.body.style.backgroundColor = 'red'">RED</button>
  <button onclick="document.body.style.backgroundColor = 'green'">GREEN</button>

</body>
</html>
```

---

### **Q15: Write JavaScript code to set a cookie in the user’s computer.**

```javascript
function setCookie(name, value, days) {
  let date = new Date();
  date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
  let expires = "expires=" + date.toUTCString();
  document.cookie = name + "=" + value + ";" + expires + ";path=/";
}

// Example usage:
setCookie("username", "JohnDoe", 7);
```

---

### **Q16: Write a JavaScript program to accept two numbers and display their sum using a pop-up box.**

```javascript
let num1 = parseFloat(prompt("Enter the first number:"));
let num2 = parseFloat(prompt("Enter the second number:"));
alert("The sum is: " + (num1 + num2));
```

---

### **Q17: Write a JavaScript program that changes the background color of the page by refreshing the page every 2 seconds.**

```html
<!DOCTYPE html>
<html>
<head>
  <title>Auto Background Change</title>
</head>
<body>

  <script>
    const colors = ['red', 'blue', 'green', 'yellow'];
    let index = 0;

    function changeColor() {
      document.body.style.backgroundColor = colors[index];
      index = (index + 1) % colors.length;
    }

    setInterval(changeColor, 2000);
  </script>

</body>
</html>
```

---

### **Q18: Write the code to process online alumni information for your college.**

```html
<!DOCTYPE html>
<html>
<head>
  <title>Alumni Form</title>
</head>
<body>
  <form onsubmit="return validateForm()">
    Name: <input type="text" id="name"><br><br>
    Date of Birth: <input type="date" id="dob"><br><br>
    Email: <input type="email" id="email"><br><br>
    Password: <input type="password" id="password"><br><br>
    Hobbies: <br>
    <input type="checkbox" value="Reading"> Reading<br>
    <input type="checkbox" value="Sports"> Sports<br><br>
    Branch: <br>
    <input type="radio" name="branch" value="CSE"> CSE<br>
    <input type="radio" name="branch" value="ECE"> ECE<br><br>
    <input type="submit" value="Submit">
  </form>

  <script>
    function validateForm() {
      let name = document.getElementById("name").value;
      let dob = document.getElementById("dob").value;
      let email = document.getElementById("email").value;
      let password = document.getElementById("password").value;
      let age = new Date().getFullYear() - new Date(dob).getFullYear();

      if (!name || !dob || !email || !password) {
        alert("All fields are required.");
        return false;
      }

      if (!email.includes('@') || !email.includes('.')) {
        alert("Please enter a valid email.");
        return false;
      }

      if (age < 22) {
        alert("You must be at least 22 years old.");
        return false;
      }

      if (password.length < 6) {
        alert("Password must be at least 6 characters.");
        return false;
      }

      return true;
    }
  </script>

</body>
</html>
```

--- 

These cover all the JavaScript questions from your list!
